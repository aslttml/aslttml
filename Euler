N=input()
Primes=()
Rem=n
A=2
While(a<=rem):
  Try:
   For x in primes:
     If a%x==0:
       A=a+2
       Break
   Except:
      Continue
 If rem%a==0:
   While(rem%a==0 and rem>1):
      Rem=rem/a
      Print(rem) 
   Primes. Append(a)
 If a==2:
  A=a+1
 Else:
  A=a+2
Print(primes) 
Print(primes.pop())
